# Dev only — Vite dev server (port 5174)
VITE_API_URL=http://127.0.0.1:9000

# Docker production uses same-origin proxy; set API_BACKEND in docker-compose instead:
# API_BACKEND=host.docker.internal:9000
